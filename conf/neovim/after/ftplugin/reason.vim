let g:LanguageClient_serverCommands = {
\ 'reason': ['ocaml-language-server', '--stdio'],
\ }

" does not work for revery etc
" let g:LanguageClient_serverCommands = {
" \ 'reason': ['$HOME/reason-language-server.exe']
" \ }
